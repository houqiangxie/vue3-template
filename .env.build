VITE_baseUrl = '/api'
NODE_ENV = production
VITE_MODE = 'production'
VITE_outputDir="dist"

# 打包是否清空 dist（true=清空；false=追加覆盖，便于单端增量合到同一 dist）
VITE_EMPTY_OUTDIR=true

# 登录 AES：构建密钥放 .env.build.local
# VITE_LOGIN_AES_KEY=
# VITE_LOGIN_AES_IV=
